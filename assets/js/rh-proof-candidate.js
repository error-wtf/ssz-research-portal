(() => {
  const $ = (id) => document.getElementById(id);
  function setupCanvas(id) { const c=$(id), d=devicePixelRatio||1; const r=c.getBoundingClientRect(); c.width=Math.max(420,r.width*d); c.height=Math.max(260,r.height*d); const x=c.getContext('2d'); x.setTransform(d,0,0,d,0,0); return {c,x,w:r.width,h:r.height}; }
  function axes(x,w,h) { x.strokeStyle='#cbd5e1'; x.lineWidth=1; x.beginPath(); x.moveTo(42,14); x.lineTo(42,h-32); x.lineTo(w-14,h-32); x.stroke(); x.fillStyle='#64748b'; x.font='12px system-ui'; x.fillText('radius / parameter',w-125,h-10); }
  function drawTheta() { const {x,w,h}=setupCanvas('rh-theta-canvas'); x.clearRect(0,0,w,h); axes(x,w,h); const beta=+($('rh-beta').value); const N=180; const vals=[]; for(let i=0;i<N;i++){const t=i/(N-1)*4; vals.push(Math.exp(-Math.PI*Math.exp(2*t)));} const max=Math.max(...vals); x.strokeStyle='#b8860b'; x.lineWidth=2.5; x.beginPath(); vals.forEach((v,i)=>{const px=42+i/(N-1)*(w-58),py=h-32-(v/max)*(h-54);i?x.lineTo(px,py):x.moveTo(px,py);});x.stroke(); x.strokeStyle='#2563eb';x.setLineDash([5,4]); const threshold=h-32-Math.exp(-2*beta)*Math.max(12,h*.12);x.beginPath();x.moveTo(42,threshold);x.lineTo(w-14,threshold);x.stroke();x.setLineDash([]); x.fillStyle='#64748b';x.fillText('positive Gaussian source θ(x)',50,25);x.fillText('weighted reference',w-125,threshold-7); }
  function drawDecay() { const {x,w,h}=setupCanvas('rh-decay-canvas');x.clearRect(0,0,w,h);axes(x,w,h);const b=+$('rh-decay-beta').value,a=+$('rh-alpha').value;const m=7.99999995,p0=19.999996,C=1/(m-b)**2+(1/p0)*(1+m/(m-b)+a/(m-b))**2; $('rh-decay-beta-out').textContent=b.toFixed(3);$('rh-alpha-out').textContent=a.toFixed(1);$('rh-C-out').textContent=C.toFixed(3);x.strokeStyle='#2563eb';x.lineWidth=2.5;x.beginPath();for(let i=0;i<=180;i++){const R=i/180*12,v=Math.exp(-2*b*R),px=42+i/180*(w-58),py=h-32-v*(h-54);i?x.lineTo(px,py):x.moveTo(px,py);}x.stroke();x.fillStyle='#64748b';x.fillText('Cα,β exp(−2βR)',50,25);x.fillText('R → ∞',w-58,h-10); }
  function drawMatrix() { const {x,w,h}=setupCanvas('rh-matrix-canvas');x.clearRect(0,0,w,h);const b=+$('rh-matrix-beta').value,k=+$('rh-matrix-k').value;const g=1+k*(1-b)-k*k/(2*Math.max(b,.01));$('rh-matrix-beta-out').textContent=b.toFixed(2);$('rh-matrix-k-out').textContent=k.toFixed(2);$('rh-g-out').textContent=g.toFixed(3);[['2β',2*b],['ik',k],['−ik',k],['Gβ/P',g]].forEach((c,i)=>{const px=30+(i%2)*(w/2-20),py=24+Math.floor(i/2)*(h/2-16);x.fillStyle=c[1]>0?'#087f5b':'#b42318';x.globalAlpha=.12;x.fillRect(px,py,w/2-45,h/2-28);x.globalAlpha=1;x.fillStyle=c[1]>0?'#087f5b':'#b42318';x.font='700 18px ui-monospace';x.fillText(c[0],px+18,py+34);x.font='12px system-ui';x.fillText(c[1]>0?'positive region':'not certified',px+18,py+55);});}
  function zetaApprox(sigma,t,N){let re=0,im=0;for(let n=1;n<=N;n++){const a=Math.pow(n,-sigma),ph=t*Math.log(n),sg=n%2?1:-1;re+=sg*a*Math.cos(ph);im-=sg*a*Math.sin(ph);}const denRe=1-2*Math.pow(2,1-sigma)*Math.cos(t*Math.log(2)),denIm=2*Math.pow(2,1-sigma)*Math.sin(t*Math.log(2));const d=denRe*denRe+denIm*denIm;if(d<1e-14)return {re:1e4,im:0,pole:true};return {re:(re*denRe+im*denIm)/d,im:(im*denRe-re*denIm)/d,pole:false};}
  function drawZeta(){const c=$('rh-zeta-canvas');if(!c)return;const {x,w,h}=setupCanvas('rh-zeta-canvas');const sigma=+$('rh-zeta-sigma').value,t0=+$('rh-zeta-t').value,N=+$('rh-zeta-n').value;const span=8,pts=180,vals=[];for(let i=0;i<=pts;i++){const t=t0-span/2+span*i/pts;vals.push(zetaApprox(sigma,t,N));}const scale=Math.max(1,...vals.map(v=>Math.hypot(v.re,v.im)));x.clearRect(0,0,w,h);axes(x,w,h);[['re','#b8860b','Re ζ'],['im','#2563eb','Im ζ']].forEach(([key,color,label])=>{x.strokeStyle=color;x.lineWidth=2;x.beginPath();vals.forEach((v,i)=>{const px=42+i/pts*(w-58),py=h/2-v[key]/scale*(h*.38);i?x.lineTo(px,py):x.moveTo(px,py);});x.stroke();x.fillStyle=color;x.fillText(label,50,key==='re'?25:42);});const cur=zetaApprox(sigma,t0,N),px=w/2,py=h/2-cur.re/scale*(h*.38);x.fillStyle='#d9480f';x.beginPath();x.arc(px,py,6,0,Math.PI*2);x.fill();$('rh-zeta-t-out').textContent=t0.toFixed(1);$('rh-zeta-sigma-out').textContent=sigma.toFixed(2);$('rh-zeta-n-out').textContent=N;$('rh-zeta-value-out').textContent=Math.hypot(cur.re,cur.im).toFixed(3);$('rh-zeta-status-out').textContent=cur.pole?'near the zeta pole — clipped visual proxy':Math.hypot(cur.re,cur.im)<.15?'near-zero visual sample':'finite approximation';}
  let dirichletFrame=1; function drawDirichlet(){const c=$('rh-dirichlet-canvas');if(!c)return;const {x,w,h}=setupCanvas('rh-dirichlet-canvas');const sigma=+$('rh-zeta-sigma').value,t=+$('rh-zeta-t').value,N=+$('rh-zeta-n').value,k=Math.max(1,Math.min(N,Math.floor(dirichletFrame)));const pts=[];let re=0,im=0,max=1;for(let n=1;n<=N;n++){const a=Math.pow(n,-sigma),ph=t*Math.log(n),tr=a*Math.cos(ph),ti=-a*Math.sin(ph),prev={re,im};re+=tr;im+=ti;pts.push({re,im,prev,tr,ti,a,ph});max=Math.max(max,Math.hypot(re,im));}const scale=Math.min((w-110)/(2*max),(h-100)/(2*max));const ox=w/2,oy=h/2;x.clearRect(0,0,w,h);x.fillStyle='#f8fafc';x.fillRect(0,0,w,h);x.strokeStyle='#dbe4ee';x.lineWidth=1;for(let g=-4;g<=4;g++){const gx=ox+g*scale,gy=oy-g*scale;x.beginPath();x.moveTo(gx,18);x.lineTo(gx,h-34);x.moveTo(42,gy);x.lineTo(w-14,gy);x.stroke();}x.strokeStyle='#334155';x.beginPath();x.moveTo(42,oy);x.lineTo(w-14,oy);x.moveTo(ox,18);x.lineTo(ox,h-34);x.stroke();x.fillStyle='#64748b';x.font='12px system-ui';x.fillText('Re',w-38,oy-8);x.fillText('Im',ox+8,28);const origin={re:0,im:0};pts.slice(0,k).forEach((p,i)=>{const sx=ox+p.prev.re*scale,sy=oy-p.prev.im*scale,ex=ox+p.re*scale,ey=oy-p.im*scale;x.strokeStyle=i===k-1?'#2563eb':'#b8860b';x.globalAlpha=i===k-1?1:.72;x.lineWidth=i===k-1?3:1.7;x.beginPath();x.moveTo(sx,sy);x.lineTo(ex,ey);x.stroke();if(i===k-1){x.strokeStyle='#d9480f';x.lineWidth=1.4;x.setLineDash([4,3]);x.beginPath();x.moveTo(ox,oy);x.lineTo(ox+p.tr*scale,oy-p.ti*scale);x.stroke();x.setLineDash([]);}});x.globalAlpha=1;const cur=pts[k-1],cx=ox+cur.re*scale,cy=oy-cur.im*scale;x.fillStyle='#d9480f';x.beginPath();x.arc(cx,cy,6,0,Math.PI*2);x.fill();x.fillStyle='#0f172a';x.font='600 13px system-ui';x.fillText(`S${k} = ${cur.re.toFixed(3)} ${cur.im<0?'−':'+'} ${Math.abs(cur.im).toFixed(3)}i`,50,26);x.font='12px system-ui';x.fillStyle='#475569';x.fillText(`term ${k}: |n⁻ˢ|=${cur.a.toFixed(4)}, angle=${(-cur.ph%(2*Math.PI)).toFixed(3)} rad`,50,h-12);x.fillStyle='#b8860b';x.fillText('partial-sum path',58,44);x.fillStyle='#2563eb';x.fillText('current term vector',w-150,44);}
  let preparedDirichlet={key:'',pts:[],max:1}; function prepareDirichlet(sigma,t,N){const key=`${sigma}|${t}|${N}`;if(preparedDirichlet.key===key)return preparedDirichlet;const pts=[];let re=0,im=0,max=1;for(let n=1;n<=N;n++){const a=Math.pow(n,-sigma),ph=t*Math.log(n),tr=a*Math.cos(ph),ti=-a*Math.sin(ph),prev={re,im};re+=tr;im+=ti;pts.push({re,im,prev,tr,ti,a,ph});max=Math.max(max,Math.hypot(re,im));}preparedDirichlet={key,pts,max};return preparedDirichlet;}
  function drawPreparedDirichlet(){const c=$('rh-dirichlet-canvas');if(!c)return;const {x,w,h}=setupCanvas('rh-dirichlet-canvas');const sigma=+$('rh-zeta-sigma').value,t=+$('rh-zeta-t').value,N=+$('rh-zeta-n').value,k=Math.max(1,Math.min(N,Math.floor(dirichletFrame))),data=prepareDirichlet(sigma,t,N),pts=data.pts,scale=Math.min((w-110)/(2*data.max),(h-100)/(2*data.max)),ox=w/2,oy=h/2;x.clearRect(0,0,w,h);x.fillStyle='#f8fafc';x.fillRect(0,0,w,h);x.strokeStyle='#dbe4ee';x.lineWidth=1;for(let g=-4;g<=4;g++){const gx=ox+g*scale,gy=oy-g*scale;x.beginPath();x.moveTo(gx,18);x.lineTo(gx,h-34);x.moveTo(42,gy);x.lineTo(w-14,gy);x.stroke();}x.strokeStyle='#334155';x.beginPath();x.moveTo(42,oy);x.lineTo(w-14,oy);x.moveTo(ox,18);x.lineTo(ox,h-34);x.stroke();x.fillStyle='#64748b';x.font='12px system-ui';x.fillText('Re',w-38,oy-8);x.fillText('Im',ox+8,28);pts.slice(0,k).forEach((p,i)=>{const sx=ox+p.prev.re*scale,sy=oy-p.prev.im*scale,ex=ox+p.re*scale,ey=oy-p.im*scale;x.strokeStyle=i===k-1?'#2563eb':'#b8860b';x.globalAlpha=i===k-1?1:.72;x.lineWidth=i===k-1?3:1.7;x.beginPath();x.moveTo(sx,sy);x.lineTo(ex,ey);x.stroke();if(i===k-1){x.strokeStyle='#d9480f';x.lineWidth=1.4;x.setLineDash([4,3]);x.beginPath();x.moveTo(ox,oy);x.lineTo(ox+p.tr*scale,oy-p.ti*scale);x.stroke();x.setLineDash([]);}});x.globalAlpha=1;const cur=pts[k-1],cx=ox+cur.re*scale,cy=oy-cur.im*scale;x.fillStyle='#d9480f';x.beginPath();x.arc(cx,cy,6,0,Math.PI*2);x.fill();x.fillStyle='#0f172a';x.font='600 13px system-ui';x.fillText(`S${k} = ${cur.re.toFixed(3)} ${cur.im<0?'−':'+'} ${Math.abs(cur.im).toFixed(3)}i`,50,26);x.font='12px system-ui';x.fillStyle='#475569';x.fillText(`term ${k}: |n⁻ˢ|=${cur.a.toFixed(4)}, angle=${(-cur.ph%(2*Math.PI)).toFixed(3)} rad · precomputed N=${N}`,50,h-12);x.fillStyle='#b8860b';x.fillText('partial-sum path',58,44);x.fillStyle='#2563eb';x.fillText('current term vector',w-150,44);}
  function drawReferenceDirichlet(){const c=$('rh-dirichlet-canvas');if(!c)return;const {x,w,h}=setupCanvas('rh-dirichlet-canvas');const sigma=+$('rh-zeta-sigma').value,t=+$('rh-zeta-t').value,N=+$('rh-zeta-n').value,k=Math.max(1,Math.min(N,Math.floor(dirichletFrame))),data=prepareDirichlet(sigma,t,N),pts=data.pts,scale=Math.min((w-100)/(2*Math.max(2,data.max)),(h-90)/(2*Math.max(2,data.max))),ox=w/2,oy=h/2;x.clearRect(0,0,w,h);x.fillStyle='#050b13';x.fillRect(0,0,w,h);x.strokeStyle='#12364a';x.lineWidth=1;for(let g=-6;g<=6;g++){const gx=ox+g*scale,gy=oy-g*scale;x.beginPath();x.moveTo(gx,18);x.lineTo(gx,h-34);x.moveTo(42,gy);x.lineTo(w-14,gy);x.stroke();}x.strokeStyle='#8aa4b7';x.lineWidth=1.2;x.beginPath();x.moveTo(42,oy);x.lineTo(w-14,oy);x.moveTo(ox,18);x.lineTo(ox,h-34);x.stroke();const palette=['#f5df4d','#e7a1b4','#a9c3c8','#b8b1f0'];const sweep=3.6;for(let n=1;n<=Math.min(N,80);n++){const radius=Math.pow(n,-sigma)*scale;x.strokeStyle=palette[n%palette.length];x.globalAlpha=n===1?.75:.22;x.lineWidth=n<6?1.4:.8;x.beginPath();for(let j=0;j<=64;j++){const tau=t-sweep/2+sweep*j/64,px=ox+radius*Math.cos(-tau*Math.log(n)),py=oy-radius*Math.sin(-tau*Math.log(n));j?x.lineTo(px,py):x.moveTo(px,py);}x.stroke();}x.globalAlpha=1;x.strokeStyle='#f8a4c2';x.lineWidth=2.2;x.beginPath();pts.slice(0,k).forEach((p,i)=>{const px=ox+p.re*scale,py=oy-p.im*scale;i?x.lineTo(px,py):x.moveTo(px,py);});x.stroke();const cur=pts[k-1],cx=ox+cur.re*scale,cy=oy-cur.im*scale;x.strokeStyle='#37bdf8';x.lineWidth=2;x.beginPath();x.moveTo(ox+cur.prev.re*scale,oy-cur.prev.im*scale);x.lineTo(cx,cy);x.stroke();x.fillStyle='#ffd84d';x.beginPath();x.arc(cx,cy,5,0,Math.PI*2);x.fill();x.fillStyle='#f1f5f9';x.font='600 13px system-ui';x.fillText('Riemann zeta · finite term orbits + partial sums',50,27);x.font='12px system-ui';x.fillText(`σ=${sigma.toFixed(2)}  t=${t.toFixed(2)}  S${k}=${cur.re.toFixed(3)} ${cur.im<0?'−':'+'} ${Math.abs(cur.im).toFixed(3)}i`,50,h-12);x.fillStyle='#b8b1f0';x.fillText('term orbits',w-95,27);x.fillStyle='#f8a4c2';x.fillText('Sₖ path',w-95,44);}
  let fullMapFrame=0;function drawFullDirichlet(){const c=$('rh-dirichlet-canvas');if(!c)return;const {x,w,h}=setupCanvas('rh-dirichlet-canvas');const sigma=+$('rh-zeta-sigma').value,t=+$('rh-zeta-t').value,N=+$('rh-zeta-n').value,k=Math.max(1,Math.min(N,Math.floor(dirichletFrame))),data=prepareDirichlet(sigma,t,N),scale=Math.min((w-100)/14,(h-90)/7.6),ox=w/2,oy=h/2;x.clearRect(0,0,w,h);x.fillStyle='#02070d';x.fillRect(0,0,w,h);x.strokeStyle='#12435a';x.lineWidth=1;for(let g=-7;g<=7;g++){const gx=ox+g*scale,gy=oy-g*scale;x.beginPath();x.moveTo(gx,16);x.lineTo(gx,h-34);x.moveTo(42,gy);x.lineTo(w-14,gy);x.stroke();}x.strokeStyle='#a9b9c8';x.lineWidth=1.1;x.beginPath();x.moveTo(42,oy);x.lineTo(w-14,oy);x.moveTo(ox,16);x.lineTo(ox,h-34);x.stroke();const pal=['#f5df4d','#e7a1b4','#a9c3c8','#b8b1f0'];const sigmas=[];for(let j=0;j<14;j++)sigmas.push(-1.1+j*2.1/13);const rays=[];for(let n=2;n<=Math.min(N,24);n++){const color=pal[n%pal.length];for(const sg of sigmas){const r=Math.pow(n,-sg)*scale;x.strokeStyle=color;x.globalAlpha=.25;x.lineWidth=.75;x.beginPath();for(let j=0;j<=100;j++){const a=-Math.PI+2*Math.PI*j/100,px=ox+r*Math.cos(a),py=oy-r*Math.sin(a);j?x.lineTo(px,py):x.moveTo(px,py);}x.stroke();}for(let j=0;j<18;j++){const tv=-4+j*8/17,a=-(tv+(t-(+$('rh-zeta-t').value))*.25)*Math.log(n);rays.push({n,tv,a,color});x.strokeStyle=color;x.globalAlpha=.2;x.beginPath();x.moveTo(ox,oy);x.lineTo(ox+7.2*scale*Math.cos(a),oy-7.2*scale*Math.sin(a));x.stroke();}}x.globalAlpha=1;const pts=data.pts;const cur=pts[k-1],cx=ox+cur.re*scale,cy=oy-cur.im*scale;x.strokeStyle='#37bdf8';x.lineWidth=2;x.beginPath();x.moveTo(ox+cur.prev.re*scale,oy-cur.prev.im*scale);x.lineTo(cx,cy);x.stroke();x.fillStyle='#ffd84d';x.beginPath();x.arc(cx,cy,6,0,Math.PI*2);x.fill();x.fillStyle='#f8a4c2';x.lineWidth=2;x.beginPath();pts.forEach((p,i)=>{const px=ox+p.re*scale,py=oy-p.im*scale;i?x.lineTo(px,py):x.moveTo(px,py);});x.stroke();x.fillStyle='#f8fafc';x.font='600 13px system-ui';x.fillText('Riemann zeta function · complete finite term map',50,26);x.font='12px system-ui';x.fillText(`σ=${sigma.toFixed(2)}  t=${t.toFixed(2)}  S${k}=${cur.re.toFixed(3)} ${cur.im<0?'−':'+'} ${Math.abs(cur.im).toFixed(3)}i`,50,h-12);fullMapFrame=(fullMapFrame+1)%720;}
  function animateDirichlet(){const n=+$('rh-zeta-n')?.value||160;dirichletFrame=dirichletFrame>=n?1:dirichletFrame+1;drawFullDirichlet();requestAnimationFrame(animateDirichlet);}
  let phaseFrame=0; function drawPhase(){const c=$('rh-phase-canvas');if(!c)return;const {x,w,h}=setupCanvas('rh-phase-canvas');const primes=[2,3,5],t=phaseFrame*.045,wrap=v=>(v+Math.PI)%(2*Math.PI)-Math.PI,px=wrap(t*Math.log(2)),py=wrap(t*Math.log(3));x.clearRect(0,0,w,h);const ox=w/2,oy=h/2,s=Math.min((w-90)/(2*Math.PI*2),(h-80)/(2*Math.PI*2));x.strokeStyle='#334155';x.lineWidth=1;x.strokeRect(ox-Math.PI*s,oy-Math.PI*s,2*Math.PI*s,2*Math.PI*s);x.strokeStyle='#b8860b';x.lineWidth=2;x.beginPath();for(let i=0;i<=phaseFrame;i++){const tt=i*.045,qx=ox+wrap(tt*Math.log(2))*s,qy=oy-wrap(tt*Math.log(3))*s;i?x.lineTo(qx,qy):x.moveTo(qx,qy);}x.stroke();x.fillStyle='#38bdf8';x.beginPath();x.arc(ox+px*s,oy-py*s,6,0,Math.PI*2);x.fill();const score=Math.max(...primes.map(p=>Math.hypot(Math.cos(t*Math.log(p))-1,Math.sin(t*Math.log(p)))));x.fillStyle='#64748b';x.font='12px system-ui';x.fillText(`t=${t.toFixed(2)} · return score (2,3,5)=${score.toFixed(3)}`,50,25);x.fillText('phase log 2',w-90,oy-8);x.fillText('phase log 3',ox+8,20);phaseFrame=(phaseFrame+1)%720;requestAnimationFrame(drawPhase);}
  function drawPlane(){const c=$('rh-plane-canvas');if(!c)return;const {x,w,h}=setupCanvas('rh-plane-canvas');x.clearRect(0,0,w,h);axes(x,w,h);const bx=+$('rh-plane-x').value,b=+$('rh-plane-beta').value;$('rh-plane-x-out').textContent=bx.toFixed(1);$('rh-plane-beta-out').textContent=b.toFixed(2);$('rh-plane-s-out').textContent=(.5-b).toFixed(3);$('rh-plane-status-out').textContent=b>0&&b<.5?'Weyl-tested half-strip':b<0?'reflection half-strip':'critical line / boundary';const ox=42,oy=h-32,sx=(w-58)/16,sy=(h-54)/1.2;x.fillStyle='#e9c46a';x.fillRect(ox,oy-sy*.5,w-58,sy*.5);x.fillStyle='#264653';x.fillRect(ox,oy-sy*1.0,w-58,sy*.5);x.strokeStyle='#b8860b';x.lineWidth=2;x.beginPath();x.moveTo(ox,oy-sy*.5);x.lineTo(w-14,oy-sy*.5);x.stroke();x.strokeStyle='#7c3aed';x.setLineDash([5,4]);x.beginPath();x.moveTo(ox+8*sx,oy);x.lineTo(ox+8*sx,14);x.stroke();x.setLineDash([]);const px=ox+(bx+8)*sx,py=oy-b*sy;x.fillStyle='#d9480f';x.beginPath();x.arc(px,py,6,0,Math.PI*2);x.fill();x.fillStyle='#64748b';x.fillText('Im α = β',50,25);x.fillText('Re α = ξ',w-86,h-10);x.fillText('critical line β=0',w-150,oy-sy*.5-8);}
  const derivations=[['Positive source → finite weighted tails','\\[\\theta>0,\\quad e^{|\\beta||x|}\\theta\\in L^1(\\mathbb R).\\]','The Gaussian majorant gives a finite weighted integral, so both Volterra tails are genuine functions.'],['Volterra ODE','\\[u_\\pm\\prime+i\\alpha u_\\pm=\\theta.\\]','Differentiate the variable-limit integrals; the boundary term supplies the source.'],['Hermitian residue','\\[H_+=J_+\\prime+A_\\alpha^*J_++J_+A_\\alpha,\\quad H_+>0.\\]','The matrix identity turns scalar profile margins into positive production for the actual state.'],['Global Green balance','\\[M_-(0)-M_+(0)=E_-+E_+.\\]','Finite identities plus vanishing endpoint flux produce the improper identity with fixed orientations.'],['Contradiction','\\[0=M_-(0)-M_+(0)=E_-+E_+>0.\\]','Xi-zero matching forces the origin balance to vanish, while nondegeneracy makes production strictly positive.']];
  function bindDerivation(){const panel=document.querySelector('[data-derivation-panel]');if(!panel)return;const tabs=[...panel.querySelectorAll('[data-derivation]')],idx=panel.querySelector('.derivation-index'),title=panel.querySelector('[data-derivation-title]'),eq=panel.querySelector('[data-derivation-equation]'),text=panel.querySelector('[data-derivation-text]');tabs.forEach(t=>t.addEventListener('click',()=>{const i=+t.dataset.derivation;tabs.forEach(n=>n.classList.toggle('is-active',n===t));idx.textContent=`Step ${i+1} / ${derivations.length}`;title.textContent=derivations[i][0];eq.innerHTML=derivations[i][1];text.textContent=derivations[i][2];if(window.MathJax&&MathJax.typesetPromise)MathJax.typesetPromise([eq]);}));}
  let zetaTimer; function toggleZeta(){const b=$('rh-zeta-play');if(!b)return;if(zetaTimer){clearInterval(zetaTimer);zetaTimer=null;b.textContent='▶ Animate t';return;}b.textContent='⏸ Pause t';zetaTimer=setInterval(()=>{const t=$('rh-zeta-t');t.value=(+t.value+.2)%60;drawZeta();drawReferenceDirichlet();},80);}
  let flowTimer; function animateFlow(){const nodes=[...document.querySelectorAll('.rh-flow .node')];let i=0;clearInterval(flowTimer);flowTimer=setInterval(()=>{nodes.forEach(n=>n.classList.remove('active'));nodes[i%nodes.length].classList.add('active');i++;},850);}
  function bind(id,fn){const e=$(id);if(e)e.addEventListener('input',fn);}
  window.addEventListener('resize',()=>{drawTheta();drawDecay();drawMatrix();drawPlane();drawZeta();drawFullDirichlet();});
  bind('rh-beta',drawTheta);bind('rh-decay-beta',drawDecay);bind('rh-alpha',drawDecay);bind('rh-matrix-beta',drawMatrix);bind('rh-matrix-k',drawMatrix);bind('rh-plane-x',drawPlane);bind('rh-plane-beta',drawPlane);bind('rh-zeta-t',()=>{drawZeta();drawFullDirichlet();});bind('rh-zeta-sigma',()=>{drawZeta();drawFullDirichlet();});bind('rh-zeta-n',()=>{drawZeta();drawFullDirichlet();});if($('rh-zeta-play'))$('rh-zeta-play').addEventListener('click',toggleZeta);
  /* Final full-curve renderer: every declared ring and ray is fitted inside
     the canvas, while the highlighted S_N trajectory is sampled across the
     entire t-window rather than showing only a moving prefix. */
  drawFullDirichlet=function(){const c=$('rh-dirichlet-canvas');if(!c)return;const {x,w,h}=setupCanvas('rh-dirichlet-canvas');const sigma=+$('rh-zeta-sigma').value,t0=+$('rh-zeta-t').value,N=+$('rh-zeta-n').value;const terms=Math.min(N,24),sigmaMin=-.35,sigmaMax=1.05,maxRadius=Math.pow(terms,-sigmaMin),scale=Math.min((w-100)/(2*maxRadius),(h-78)/(2*maxRadius)),ox=w/2,oy=h/2;x.clearRect(0,0,w,h);x.fillStyle='#02070d';x.fillRect(0,0,w,h);x.strokeStyle='#12435a';x.lineWidth=1;for(let g=-Math.ceil(maxRadius);g<=Math.ceil(maxRadius);g++){const gx=ox+g*scale,gy=oy-g*scale;x.beginPath();x.moveTo(gx,16);x.lineTo(gx,h-34);x.moveTo(42,gy);x.lineTo(w-14,gy);x.stroke();}x.strokeStyle='#a9b9c8';x.lineWidth=1.1;x.beginPath();x.moveTo(42,oy);x.lineTo(w-14,oy);x.moveTo(ox,16);x.lineTo(ox,h-34);x.stroke();const pal=['#f5df4d','#e7a1b4','#a9c3c8','#b8b1f0'];for(let n=2;n<=terms;n++){const col=pal[n%pal.length];for(let j=0;j<14;j++){const sg=sigmaMin+(sigmaMax-sigmaMin)*j/13,r=Math.pow(n,-sg)*scale;x.strokeStyle=col;x.globalAlpha=.3;x.lineWidth=.8;x.beginPath();for(let q=0;q<=180;q++){const a=2*Math.PI*q/180,px=ox+r*Math.cos(a),py=oy-r*Math.sin(a);q?x.lineTo(px,py):x.moveTo(px,py);}x.stroke();}for(let j=0;j<18;j++){const tv=-4+8*j/17,a=-tv*Math.log(n);x.strokeStyle=col;x.globalAlpha=.28;x.lineWidth=.8;x.beginPath();x.moveTo(ox,oy);x.lineTo(ox+maxRadius*scale*Math.cos(a),oy-maxRadius*scale*Math.sin(a));x.stroke();}}x.globalAlpha=1;const samples=360,span=8;x.strokeStyle='#f8a4c2';x.lineWidth=2.4;x.beginPath();for(let q=0;q<=samples;q++){const tt=t0-span/2+span*q/samples;let re=0,im=0;for(let n=1;n<=N;n++){const a=Math.pow(n,-sigma),ph=tt*Math.log(n);re+=a*Math.cos(ph);im-=a*Math.sin(ph);}const px=ox+re*scale,py=oy-im*scale;q?x.lineTo(px,py):x.moveTo(px,py);}x.stroke();let re=0,im=0;for(let n=1;n<=N;n++){const a=Math.pow(n,-sigma),ph=t0*Math.log(n);re+=a*Math.cos(ph);im-=a*Math.sin(ph);}const cx=ox+re*scale,cy=oy-im*scale;x.fillStyle='#ffd84d';x.beginPath();x.arc(cx,cy,6,0,Math.PI*2);x.fill();x.fillStyle='#f8fafc';x.font='600 13px system-ui';x.fillText('Riemann zeta · complete fitted term map + full S_N(t) curve',50,26);x.font='12px system-ui';x.fillText(`σ=${sigma.toFixed(2)}  t=${t0.toFixed(2)}  N=${N}  full t-window=[${(t0-span/2).toFixed(1)}, ${(t0+span/2).toFixed(1)}]`,50,h-12);};
  /* The canonical portal view intentionally omits rays: closed term curves
     and the complete smooth S_N(t) trajectory are the interactive surface. */
  drawFullDirichlet=function(){const c=$('rh-dirichlet-canvas');if(!c)return;const {x,w,h}=setupCanvas('rh-dirichlet-canvas'),sigma=+$('rh-zeta-sigma').value,t0=+$('rh-zeta-t').value,N=+$('rh-zeta-n').value,terms=Math.min(N,24),radii=[.32,.48,.72,1,1.35,1.8,2.35,3,3.8,4.7,5.7,6.55],scale=Math.min((w-100)/13.1,(h-78)/7.1),ox=w/2,oy=h/2,pal=['#f5df4d','#e7a1b4','#a9c3c8','#b8b1f0','#8de0d0'];x.clearRect(0,0,w,h);x.fillStyle='#02070d';x.fillRect(0,0,w,h);x.strokeStyle='#12435a';x.lineWidth=1;for(let g=-7;g<=7;g++){const gx=ox+g*scale,gy=oy-g*scale;x.beginPath();x.moveTo(gx,16);x.lineTo(gx,h-34);x.moveTo(42,gy);x.lineTo(w-14,gy);x.stroke();}x.strokeStyle='#a9b9c8';x.beginPath();x.moveTo(42,oy);x.lineTo(w-14,oy);x.moveTo(ox,16);x.lineTo(ox,h-34);x.stroke();for(let n=2;n<=terms;n++){const col=pal[n%pal.length];for(const r of radii){x.strokeStyle=col;x.globalAlpha=.58;x.lineWidth=.9;x.beginPath();for(let q=0;q<=180;q++){const a=2*Math.PI*q/180,px=ox+r*scale*Math.cos(a),py=oy-r*scale*Math.sin(a);q?x.lineTo(px,py):x.moveTo(px,py);}x.stroke();}}x.globalAlpha=1;const samples=360,span=8;x.strokeStyle='#f8a4c2';x.lineWidth=2.4;x.beginPath();for(let q=0;q<=samples;q++){const tt=t0-span/2+span*q/samples;let re=0,im=0;for(let n=1;n<=N;n++){const a=Math.pow(n,-sigma),ph=tt*Math.log(n);re+=a*Math.cos(ph);im-=a*Math.sin(ph);}const px=ox+re*scale,py=oy-im*scale;q?x.lineTo(px,py):x.moveTo(px,py);}x.stroke();x.fillStyle='#f8fafc';x.font='600 13px system-ui';x.fillText('Riemann zeta · complete closed curves · no rays',50,26);x.font='12px system-ui';x.fillText(`σ=${sigma.toFixed(2)}  t=${t0.toFixed(2)}  N=${N}  full t-window`,50,h-12);};
  document.querySelectorAll('.rh-proof-step').forEach(s=>s.addEventListener('click',()=>{document.querySelectorAll('.rh-proof-step').forEach(n=>n.classList.remove('is-active'));s.classList.add('is-active');}));
  drawTheta();drawDecay();drawMatrix();drawPlane();drawZeta();drawFullDirichlet();bindDerivation();animateFlow();requestAnimationFrame(animateDirichlet);requestAnimationFrame(drawPhase);
})();
